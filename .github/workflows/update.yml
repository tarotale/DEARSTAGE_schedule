name: Daily Scraper

on:
  schedule:
    # 毎日 0時, 6時, 12時, 18時（UTC）に実行（日本時間: 9時, 15時, 21時, 3時）
    - cron: '0 15,21,3,9 * * *'
  workflow_dispatch: # 手動実行ボタン

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # Python を使って Secret から credentials.json を安全に書き出す
      - name: Create credentials.json
        env:
          GCP_CREDENTIALS: ${{ secrets.GCP_CREDENTIALS }}
        run: |
          python -c "import os, json; json.dump(json.loads(os.environ['GCP_CREDENTIALS']), open('credentials.json', 'w'))"

      # Ubuntu 24.04 互換の Xvfb パッケージ構成（libgconf-2-4 を除外）
      - name: Install System Dependencies & Xvfb
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libxi6 libgbm1

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install selenium beautifulsoup4 webdriver-manager google-api-python-client google-auth

      # Xvfb の仮想画面（1920x1080）上で scraper.py を実行
      - name: Run Scraper
        run: |
          xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" python scraper.py

      - name: Commit and Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git commit -m "Update schedule data" || exit 0
          git push
